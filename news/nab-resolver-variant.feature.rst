Add ``--use-feature=nab-resolver``, selecting nab as a third resolver variant
alongside the default resolver and the deprecated legacy resolver. The variant
is plumbing only for now and stops with a ``NotImplementedError``.
